_Bool类型，（0，1）
关系运算符的优先级低于算数运算符，高于赋值运算符
for（初始化；测试；更新），括号内存在可以不写，产生不同效果

逗号运算符
逗号运算从左到右，返回左右侧的值
while for，入口条件循环
do while 出口条件循环
至少执行一次，每次迭代后判断是否成立
分号结尾
嵌套循环，循环里有另一个循环
数组，按顺序存储的一系列类型相同的值，通过数组下标访问数组中单独的项或元素
数组元素下标从零开始，数组存储是连续的
c不会检测数组下标是否正确
用于识别数组元素的数字，下标，索引，偏移量
程序要素，接口和实现，





