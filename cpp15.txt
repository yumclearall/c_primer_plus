保证程序的健壮性，即使输入一些非法数值程序也可运行成功
实际应用中，输入于输出不匹配时常发生
友好的错误提示？
输入验证，对输入数据进行检测，确保其格式正确，符合程序要求
程序的模块化封装

函数	完成特定任务的独立代码单元
	执行某些动作，例printf（），返回一个值
	可重复利用
	传入信息，生成相应值或产生相应动作，的黑盒
函数类型 + 函数名+（参数类型 + 参数名）；

一般而言，函数原型指明了函数的返回值类型和函数接受的参数类型，这些信息称为该函数的签名
void starbar（void）；	定义形式和main（）形式相同
starbar（）声明也可写入main（）函数里面，但是不建议怎么做
也可以把他们分别放在两个文件中，
starbar（）中的变量是局部变量，只在该函数中有效
这不会引起名称冲突，他们可能是同名的不同变量

return，函数类型要与返回值相匹配

ANSI C函数原型

递归，函数调用他自己，有终止递归的条件，效率比循环更低
每级递归的变量都属于本级递归私有，对应地址不同

尾递归，最简单的递归形式，例。阶乘

















