#define定义常量，预处理指令，编译时替换，而不是运行时替换

const 限定符，限定一个变量为只读，不可更改
const int MOUNTHS = 12；，比define更灵活

明示常量
	<limits.h>	头文件中定义了一些类型的取值范围，
例，INT_MAX（int最大值），INT_MIN（最小值），FLT_MANT_DIG（float尾数位数），FLT_DIG（float最小有效数位数）

printf（）和scanf（）	I/O函数
printf（）打印指令要与类型相同，
%%	打印一个百分号
z
sizeof（）类型返回值，size_t,	%z或%zd
printf()转换说明修饰符,修饰基本的转换说明
标记，5种（-，+，空格，#，0）
	-，	待打印项左对齐，从最左端开始打印
	+，	在值面前显示正负值
	空格	有符号值若为正，前面空格，若为负，加-
	#，	把结果转换形式，
	0，	
	
数字，最小字段宽度，%4d

.数字，对于%e,%E,%f	小数点右精度
	对于%g,%G（根据数值大小，自动选择%f或%e）		有效数字最大位数
	对于%s		待打印字符最大数量
	对于整型转换	待打印数字的最小位数

		大部分的c函数都有返回值
printf（）的返回值，返回打印字符的个数，如果有输出错误，返回一个负数
检查输出错误时可能用到
打印较长字符串方法，
	分行写多个printf（），
	句尾加 \ 换行
	多个双引号

scanf（），可以读取不同格式的数据
使用格式字符串和参数列表
输入格式要严格按照引号内写入，按格式化输入
与printf（）转换格式几乎相同，输入double时用%lf
转换说明中的修饰符
	*，抑制赋值	%*d
	数字，最大字段宽度,或第一次遇到空格	%10s
	hh，把整数作为signed char或unsigned char类型读取	%hhd	%hhu
	ll，把整数作为longlong或unsignedlonglong读取	%lld %llu










