“\rsun\n”//光标移到所在行最前端存打印sun后光标移到下一行

关键概念
c会进行自动类型转换

字符串和格式化输入输出
strlen（）
const
char name[40],结尾\0存储，标记字符串结束，只能存39个字符，留下一个位置存\0
字符串，双引号，\0会默认添加在字符串末尾，不需要手动添加
c中没有专门用于存储字符串的类型

strlen（）//给出字符串中字符的长度	和sizeof（）//一字节为单位给出对象大小
返回类型通常是unsigned 或unsigned long

常量和c预处理器
#define DATA 2022	//定义一个固定的值，编译时替换，大写常量，小写变量，提高可读性










