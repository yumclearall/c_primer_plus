嵌入式系统？
开发操作系统
linux
运行快，结构紧凑
中央处理单元（cpu）	随机存取内存（ram） 外设 
CPU工作原理 从内存中获取并执行一条指令，然后获取并执行下一串指令
机器语言（0，1串）
高级编程语言
在计算机看来，高级指令就是一堆无法理解的无用数据，
编译器是把高级语言翻译成计算机能理解的机器语言指令集的程序
编译器可以把一种高级语言程序转化成供各种不同类型CPU使用的机器语言程序
c语言可移植性高
c标准	美国国家标准学会（ANSI）于1983年组建X3J11，开发了一套新标准
并于1989年正式公布，ANSIC定义了c语言和c标准库
最终版本C89货C90			c90 c11
指导原则：保持c精神，······
c是编译性语言		解释性语言（basic或面向图形界面语言）
使用c的七个步骤：（理想状态）1定义程序目标2设计程序3编写代码4编译5运行程序6测试和调试程序7维护和修改程序
文字注释
文字注释
文字注释
目标代码文件，可执行文件，库
连接器，将上面3部分合并成一个文件，寄可执行文件
集成开发环境（windows）

2 c语言概述
运算符：=
函数：main（），printf()
#include <stdio.h>		//预处理指令，包含另一个文件，stdio,标准输入输出，h，头文件
int main(void){		//main主函数，入口函数，总是第一个被调用
				void不带任何参数
int num = 1;			//变量的声明		赋值表达式语句	/**/	注释
				变量的命名要具有可读性
printf("num = %d",num);//标准输出函数	调用函数
return 0;			//返回，结束语句？
}				// {，函数体开始，}，函数体结束
编译.c链接.o生成可执行文件.exe
c语言中6种语句：标号语句，复合语句，表达式语句，选择语句，迭代语句，跳转语句，
关键字，标识符，运算符，数据
//	\n,换行	%d,占位符，以int形式输出
//	=,赋值	;,分号结束为另一条语句
//	# 在编译器接手之前处理这条命令，预处理指令
//	坚持使用标准模式
声明，int num；声明一个整形变量，并在内存中分配存储空间
int 关键字，数据类型，	num 标识符，变量，函数，或其他实体的名称，声明把特定标识符与计算机内存中特定位置联系起来，
	数据类型	标识符长度 63
用大小写字母，数字下划线命名，第一个字符不能是数字，，c语言名称区分大小写
c99 允许在需要时才声明变量，但是很多编译器可能不支持
赋值，mun = 1;	右侧赋值到左侧
printf（“”），引号里为参数，实际参数，%d，占位符,\n换行，\t,tab,\b,backspace
return ，返回值
简单程序结构
函数由函数头和函数体组成，int main（void）{声明；语句······}
提高程序可读性，每条语句占一行，注意缩进，划分层次
void buffer（void），三次，原型，调用，定义
函数原型是一种声明形式，也称为函数声明
调试（debug）编译器无法检测语义错误，语法错误会报错
关键字和保留标识符，关键字不能做标识符
auto	extern		short	while	break ······
c语义的基本模块是什么：函数函数是实现特定功能的语句块，可重复利用


















